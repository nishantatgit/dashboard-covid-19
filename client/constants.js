const svgHeight = 500;
const svgWidth = 800;
export const SVG = {
  height: svgHeight,
  width: svgWidth,
  fill: '#61808c',
  viewBox: `0 0 ${svgWidth} ${svgHeight}`, //min-x, min-y, width and height
  BAR: {
    spaceBetween: 10
  }
};
